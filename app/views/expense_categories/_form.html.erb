<%= form_with(model: expense_category, local: true) do |form| %>
  <% if expense_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(expense_category.errors.count, "error") %> prohibited this expense_category from being saved:</h2>

      <ul>
      <% expense_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :expense_category_title %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

<%#= f.hidden_field :service_id, value: @service.id %>
