<div class="modal fade" id="exampleModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="exampleModalLabel">Sign up and maximize your tax return!</h5>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
      <div class="modal-body">
        loading
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
        <button type="button" class="btn btn-primary">Finish</button>
      </div>
    </div>
  </div>
</div>
<div class="home-background-color">
  <div class="intro">
    <% if !logged_in? %>
    <% else %>

    <br>
    <%end%>
    <h1>Don't miss the opportunity to get some cash back
      <br>
      <span class="likethis">like this guy...</span></h1>

      <div class="png">
        <img src="https://68.media.tumblr.com/4dc0152b39849d158e1b5fd28fa5c731/tumblr_oq28ukUH6U1uaq136o1_1280.jpg" alt="little guy" height="370">
      </div>


      <p>Create custom expense categories and upload receipts on the go. This helps you keep your expenses
        organized and ready for tax season.
        <% if !logged_in? %>
        Sign up and see how the magic happens!
        <% else %>
        <br>
        <br>
        <%= link_to "continue organizing", expense_categories_path, class: 'btn btn-default btn-lg' %>
        <%end%>
      </p>
    </div>

    <div class="button">
      <% if !logged_in? %>
      <%= link_to "JOIN THE AWESOMENESS", new_users_path, class: 'btn btn-default btn-lg', remote: true %>
      <br><span class="login-logout"><a href="#login"><%= link_to "Already have an account? Sign in", login_path, remote: true %></a></span>
      <% else %>
      <%end%>
    </div>
  </div>
